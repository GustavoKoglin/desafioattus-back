version: "3.9"

services:
  backend:
    build: .
    container_name: desafioattus_back
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
